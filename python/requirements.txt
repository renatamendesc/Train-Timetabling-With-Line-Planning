# MIP solver library
mip==1.17

# highspy
highsbox

# gurobi solver (requires Gurobi Optimizer to be installed on the system)
# note: You also need a valid Gurobi license. Install Gurobi Optimizer first,
# then run: pip install gurobipy
gurobipy

psutil

numpy

matplotlib

